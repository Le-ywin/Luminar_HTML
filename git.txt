git status

git init >> initialize git into a repository

git add .   >> all files move into staging

git add <filename>

git commit -m "message"

git clone <repostory url> >> to generate a github repository to local machine

